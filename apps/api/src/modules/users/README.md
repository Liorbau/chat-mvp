# Users Module (Migration Scaffold)

This module is scaffolded for Week 4 Nest migration.

Current state:
- `users.placeholder.controller.ts`
- `users.placeholder.service.ts`
- `users.module.ts`

Target state:
- `users.controller.ts` (only if user endpoints are needed)
- `users.service.ts`
- `users.repository.ts` (in-memory abstraction for Week 4, DB swap seam for Week 5)
- `dto/` for request/response DTO classes when needed

Note:
- Placeholder files are temporary and should be replaced incrementally.
